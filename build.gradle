task buildHetzer(type: Exec) {
    executable = 'stack'
    args = ['build']
}

task installHetzer(dependsOn: buildHetzer, type: Exec) {
    executable = 'stack'
    args = ['install', '--local-bin-path', 'target']
}

task copyConfig(type: Copy) {
    from 'config'
    into 'target'
    include 'hetzer_conf.json'
}

task copyCss(type: Copy) {
    from 'frontend'
    into 'target'
    include '*.css'
}

task copyIndex(type: Copy) {
    from 'frontend'
    into 'target'
    include 'index.html'
}

task npmInstall(type: Exec) {
    workingDir 'frontend'
    executable = 'npm'
    args = ['install']
}

task copyBootstrapCss(dependsOn: npmInstall, type: Copy) {
    from 'frontend/node_modules/bootstrap/dist/css'
    into 'target'
    include '*.css'
}

task run(type: Exec) {
    if (file('target/hetzer').exists()) {
        workingDir './target'
        commandLine 'hetzer'
    }
    else {
        println 'Executable file does not exist. Run build first.'
    }
}

task install
install.dependsOn copyCss,copyIndex,copyConfig,installHetzer, copyBootstrapCss

